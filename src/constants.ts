const TIMEOUT = 1000 * 20 // 请求超时
const RETRY_TIMES = 3 // 重新请求次数
const MENU_CACHE_TIME_OUT = 1000 * 60 * 30 // 菜单缓存过期时间
const TRIGGLE_ASIDE = 1360 // 触发侧边栏回收的阈值

export { TIMEOUT, RETRY_TIMES, MENU_CACHE_TIME_OUT, TRIGGLE_ASIDE }
